const mysql = require('mysql2/promise'); require('dotenv').config(); async function listar() { const conn = await mysql.createConnection({ host: 'banco-gestao-gestaoemdanca.j.aivencloud.com', port: 13908, user: 'avnadmin', password: process.env.DB_PASS, database: 'defaultdb', ssl: { rejectUnauthorized: false } }); const [rows] = await conn.query('SHOW TABLES'); console.table(rows); await conn.end(); } listar();  
