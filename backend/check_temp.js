const mysql = require('mysql2/promise'); require('dotenv').config(); async function verificar() { try { const conn = await mysql.createConnection({ host: 'banco-gestao-gestaoemdanca.j.aivencloud.com', port: 13908, user: 'avnadmin', password: process.env.DB_PASS, database: 'defaultdb', ssl: { rejectUnauthorized: false } }); const [tabelas] = await conn.query('SHOW TABLES'); console.log('--- TABELAS NO BANCO ---'); console.table(tabelas); await conn.end(); } catch (e) { console.error('Erro:', e.message); } } verificar();  
